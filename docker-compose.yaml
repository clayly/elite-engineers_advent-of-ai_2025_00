services:
  advent:
    build: .
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    env_file: ".env"
    environment:
        - Z_HOURS=1
        - Z_MINUTES=22
        - Z_SECONDS=40
    restart: unless-stopped
